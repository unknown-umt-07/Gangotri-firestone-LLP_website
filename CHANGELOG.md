# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]
- Initialize project repository and add website source files
- Add CI workflow that runs `flake8` and `pytest`
- Add Render deploy workflow (requires `RENDER_API_KEY` and `RENDER_SERVICE_ID`)
- Configure branch protection and require one approving review on `main`
- Add `.flake8` config and include `flake8` in `requirements.txt`
